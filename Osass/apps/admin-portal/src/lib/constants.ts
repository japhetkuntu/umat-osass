// Shared UI constants used across multiple admin-portal pages.
// These are honorifics only - there is no backend entity for them, so they
// aren't admin-manageable data; kept in one place to avoid drift between pages.
export const TITLES = ['Mr.', 'Mrs.', 'Ms.', 'Dr.', 'Prof.'];
